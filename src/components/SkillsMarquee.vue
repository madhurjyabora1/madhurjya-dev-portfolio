<template>
  <div class="marquee" aria-hidden="true">
    <div class="marquee-track">
      <span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span><span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span><span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span><span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span><span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span><span class="marquee-seq mono">TypeScript — Node.js — Vue 3 — PostgreSQL — GraphQL — Nuxt — AWS — Docker — Cloudflare — OpenAPI —&nbsp;</span>
    </div>
  </div>
</template>
